var X = 5;
X++;
document.write(X);

var X = 5.25;
X--;
document.write(X);

window.alert(Math.random() * 100);   //This function displays a random number between 0 and 100

function myFunction(p1,p2) {
    return p1 * p2; //This function returns p1 and p2
}